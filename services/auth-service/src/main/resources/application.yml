server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:55432/auth_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true